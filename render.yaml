services:
  - type: web
    name: vidfetch
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    envVars:
      - key: PORT
        value: "10000"
      - key: FLASK_ENV
        value: production
